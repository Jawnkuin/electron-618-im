import { Tree } from 'antd';

const TreeNode = Tree.TreeNode;

class JKTreeNode extends TreeNode {
  constructor (props) {
    super(props);
  }

  render () {
    return (
      <div />
    );
  }
}
